spring.application.name=sistemasinfor

# =====================
# Database (MySQL)
# =====================
spring.datasource.url=jdbc:mysql://localhost:3307/proyectosi?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# =====================
# JPA / Hibernate
# =====================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# =====================
# Mail Configuration
# =====================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=stivenagualimpia@gmail.com
spring.mail.password=bxrsxcrhmcoxpejj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =====================
# Backup Notifications
# =====================
# Habilitar/deshabilitar notificaciones programadas de respaldo
backup.notification.enabled=true
# Destinatario de las notificaciones (puede ser el admin)
backup.notification.to=
# Mensaje a enviar
backup.notification.message=Se realizará una copia de seguridad el próximo lunes a las 09:00.
backup.notification.cron=0 0 9 * * MON

# Nota: considera mover usuario/contraseña de correo a variables de entorno
# para evitar exponer credenciales en texto plano en producción.

# =====================
# Fitness Recommendations Scheduling
# =====================
fitness.recommendation.enabled=true
fitness.recommendation.cron=0 0 10 * * MON

# =====================
# Hugging Face API (opcional, no usado actualmente)
# =====================
# huggingface.api.token=TU_TOKEN_AQUI

# =====================
# Auth lockout policy
# =====================
# Intentos máximos fallidos antes de bloquear
auth.max-attempts=3
# El desbloqueo es SIEMPRE por enlace al correo; no hay auto-desbloqueo por tiempo.
